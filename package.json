{
  "name": "lunar-orbit-explorer",
  "version": "0.1.0",
  "description": "Phase 1 proof-of-concept: two-body lunar propagator (Rust/WASM) + CesiumJS 3D visualization",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:wasm": "cd propagator && wasm-pack build --target web",
    "dev:full": "npm run build:wasm && npm run dev"
  },
  "dependencies": {
    "cesium": "^1.115.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vite-plugin-cesium": "^1.2.23"
  }
}
